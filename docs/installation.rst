============
Installation
============

At the command line::

    $ easy_install django-slds

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-slds
    $ pip install django-slds
